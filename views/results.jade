extends layout
block content
  //- each question in questions
  //-   h2 #{question.question}

  //-   each answer in question.answers
  //-     h3 #{answer.answer}
  each survey in surveys
    .row
      h2 #{survey.question_title}
      each answer in survey.answers
        .col-md-6
          h3 #{answer.answer_title}
          p #{answer.count}
