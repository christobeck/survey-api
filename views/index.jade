extends layout
block content
  header
      //- h1 #{title}
    nav.navbar-default(role="navigation")
      .navbar-header
        button(type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse")
          span.sr-only Toggle navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href="#") survey says
      .collapse.navbar-collapse.navbar-ex1-collapse
        ul.nav.navbar-nav
          li
            a(href="/surveys/create") Create a Survey
        ul.nav.navbar-nav.navbar-right
          li
            a(href="#") login

//- This is the home page of survey says!
//- use the extend keyword to include the header html in layout.jade
//- use the each keyword to iterate over the surveys object
//- create a list of survey titles using survey.title and create a link to their respective pages using a(href="surveys/survey.id")

//- see project 1 section 6.5 views/list.jade for an example
